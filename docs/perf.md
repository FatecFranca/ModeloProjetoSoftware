# Metas de performance (SPEC KIT)

- Comandos p95 < 30s (specify, clarify, analyze, plan, tasks, taskstoissues, checklist, implement, init).
- Geração de spec completa < 2min.
- Criação de issues (20 tasks) < 10s.

## Medição
- Rodar comandos em repositório exemplo e registrar tempos por execução.
- Publicar tempos coletados (p50/p95) a cada release.
